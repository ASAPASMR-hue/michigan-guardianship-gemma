# Michigan Guardianship AI Environment Variables

# =============================================================================
# REQUIRED API KEYS
# =============================================================================

# Google AI API Key (REQUIRED)
# Used for: Language model (Gemini) and cloud-based embeddings
# Get your key from: https://makersuite.google.com/app/apikey
# Note: All three variables below should have the same Google API key
GOOGLE_AI_API_KEY=your_google_api_key_here
GEMINI_API_KEY=your_google_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# HuggingFace Token (REQUIRED for local embeddings)
# Used for: Downloading embedding models for local use
# Get your token from: https://huggingface.co/settings/tokens
# Note: Both variables below should have the same HuggingFace token
HUGGING_FACE_HUB_TOKEN=your_hf_token_here
HUGGINGFACE_TOKEN=your_hf_token_here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Port Configuration
# Default: 5000 (change to 5001 on macOS to avoid AirPlay conflicts)
PORT=5001

# Model Configuration
# Set to 'true' to use smaller models (recommended for initial setup)
# - Uses all-MiniLM-L6-v2 (384 dims) instead of BAAI/bge-m3 (1024 dims)
# - Reduces memory usage from 8GB to 4GB
# - Faster but slightly less accurate
USE_SMALL_MODEL=true

# OpenRouter API Key (OPTIONAL)
# Only needed for Phase 3 testing with Mistral and Llama models
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# =============================================================================
# ADVANCED OPTIONS
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Flask debug mode (DO NOT enable in production)
FLASK_DEBUG=false

# Host configuration (default: 127.0.0.1 for local only)
HOST=127.0.0.1

# Model download timeout (seconds)
HF_HUB_DOWNLOAD_TIMEOUT=3600

# Proxy settings (if behind corporate firewall)
# HTTP_PROXY=http://your-proxy:port
# HTTPS_PROXY=http://your-proxy:port